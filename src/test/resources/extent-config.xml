<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>
        <!-- report theme -->
        <!-- standard, dark -->
        <theme>standard</theme>

        <!-- document encoding -->
        <!-- defaults to UTF-8 -->
        <encoding>UTF-8</encoding>

        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <protocol>http</protocol>

        <!-- title of the document -->
        <documentTitle>Unified automation testing report</documentTitle>

        <!-- report name - displayed at top-nav -->
        <reportName> Unified portal careerlabs automation report</reportName>

        <!-- location of charts in the test view -->
        <!-- top, bottom -->
        <testViewChartLocation>top</testViewChartLocation>
        <!-- custom javascript -->
        <scripts>
            <![CDATA[
                $(".card-title").click(function(){
    event.preventDefault();
    $(".test-wrapper row view test-view d-none").focus();
});
            ]]>
        </scripts>

        <!-- custom styles -->
        <styles>
            <![CDATA[
                
            ]]>
        </styles>
    </configuration>
</extentreports>